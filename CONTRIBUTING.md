# 기여 가이드

이 저장소의 모든 변경은 이슈에서 시작하고 Pull Request(PR) 리뷰를 거쳐 `main` 브랜치에 반영합니다.

## 1. 이슈 작성

작업 목적에 맞는 이슈 템플릿을 선택합니다.

- 버그: `[Bug] 로그인 버튼을 눌러도 반응하지 않음`
- 기능: `[Feature] 다크 모드 추가`
- 일반 작업: `[Task] README 실행 방법 보완`

한 이슈에는 하나의 목적만 작성하고, 작업 내용과 완료 조건을 확인 가능한 형태로 남깁니다. 작업을 시작하기 전에 담당자를 지정해 중복 작업을 방지합니다.

## 2. 브랜치 생성

최신 `main`에서 이슈 단위로 브랜치를 만듭니다.

```bash
git switch main
git pull origin main
git switch -c feat/#12
```

브랜치 이름은 `<type>/#<issue-number>` 형식을 사용합니다.

| Type | 용도 |
| --- | --- |
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `docs` | 문서 변경 |
| `refactor` | 동작 변경 없는 코드 개선 |
| `test` | 테스트 추가 또는 수정 |
| `chore` | 설정, 의존성 등 기타 작업 |

## 3. 커밋 작성

커밋 메시지는 `<type>: <summary> (#<issue-number>)` 형식을 사용합니다.

```text
feat: add dark mode toggle (#12)
fix: prevent duplicate form submission (#18)
docs: document local setup (#21)
```

- 요약은 영어 명령형으로 간결하게 작성합니다.
- 서로 다른 목적의 변경은 별도 커밋과 이슈로 분리합니다.

## 4. Pull Request 작성

1. 원격 브랜치에 변경 사항을 올립니다.
2. PR 제목을 `[Type] 변경 요약` 형식으로 작성합니다. 예: `[Feat] 다크 모드 추가`.
3. PR 본문에서 `Closes #12`처럼 관련 이슈를 연결합니다.
4. 변경 내용과 실제로 수행한 검증을 기록하고, UI 변경은 전후 화면을 첨부합니다.
5. 리뷰 의견을 반영하고 모든 확인 항목이 충족된 뒤 병합합니다.

작업 중인 PR은 Draft로 열고, 리뷰 가능한 상태가 되면 Ready for review로 전환합니다. 직접 `main`에 푸시하지 않습니다.

## 5. 리뷰 원칙

- 코드가 요구사항과 이슈의 완료 조건을 충족하는지 확인합니다.
- 사람보다 코드와 근거에 집중해 구체적으로 의견을 남깁니다.
- 변경 요청을 반영한 작성자는 해당 대화에 결과를 남깁니다.
- 작성자 본인의 승인만으로 병합하지 않습니다.
